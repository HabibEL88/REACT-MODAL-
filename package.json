{
  "name": "modal-projet-14-oc",
  "version": "0.1.0",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "description": "Ceci est un projet de creation d'un modal a afficher dans un projet de conertion de librairie jQuery en react",
  "keywords": ["modal", "React", "input", "form"],
  "author": "Habib EL MOUINI",
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "react-scripts start",
    "build-examples": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "build": "rimraf dist && NODE_ENV=production babel src/lib --out-dir dist --copy-files --ignore __tests__,spec.js,test.js,__snapshots__"
  },
  "devDependencies": {
    "babel-preset-react-app": "^10.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1",
    "rimraf": "^4.1.2"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "dependencies": {
    "@babel/cli": "^7.21.0",
    "react-transition-group": "^4.4.5"
  }
}
